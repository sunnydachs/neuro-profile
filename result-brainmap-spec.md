# 結果表示 新仕様（「脳マップ」導入案）

> 目的：あなたの提案「性格タイプではなく脳の情報処理スタイルとして、脳マップで
> 優位な神経システムを見せる」を、既存データ（axis_meta.json の neuro_systems）を
> 活かして実現する。科学的妥当性（推定表現の厳守）を守る。

## 根拠データ（既に実装済み）

axis_meta.json の `neuro_systems` に、各神経システムの label・region（脳部位）・
description が定義済み。これが「脳マップ」の土台。

| label | region（脳部位） | 役割 |
|---|---|---|
| 報酬・動機づけ系 | 腹側線条体を中心とする報酬回路 | 目標・報酬への接近 |
| 警戒・脅威処理系 | 扁桃体を含む | 危険・不確実への敏感さ |
| 実行制御ネットワーク | 前頭前野・頭頂葉 | 計画・集中・抑制 |
| デフォルトモード | 内側前頭・後帯状皮質など | 内省・想像・振り返り |
| サリエンス／内受容系 | 島皮質・前帯状皮質 | 身体感覚・重要情報の気づき |
| 社会認知ネットワーク | 側頭頭頂接合部など | 他者の気持ち・視点を読む |
| 情動調節系 | 前頭−辺縁調節回路 | 情動の安定・回復 |

各タイプは types.json の `neuro_top3` に、この中から3つを既に持っている。

## 新仕様（結果ページの変更）

### A. 「脳マップ」セクションを追加（③ 神経システム TOP3 の拡張）

既存の「神経システム TOP3」を、以下の「脳マップ風」に強化する。

```
🧠 あなたの脳では……

  ① 報酬・動機づけ系
     └ 腹側線条体を中心とする報酬回路
        「目標や報酬に向かって働きかける傾向」★★★★★

  ② デフォルトモード
     └ 内側前頭・後帯状皮質など
        「内省・想像・振り返りの傾向」★★★★☆

  ③ サリエンス／内受容系
     └ 島皮質・前帯状皮質
        「身体感覚や重要情報への気づき」★★★☆☆
```

- 既存の medal-list（🥇🥈🥉 + label + region + description + ★）の表示を、
  「優位な脳システム」としての語りに変える（region＝脳部位を前面に）。
- 注記（推定である旨）を必ず添える。

### B. 締めの一文（判定文）を追加

結果ページのヒーロー直下に、あなたの提案の形式で一文を置く：

> あなたの回答からは「**実行制御ネットワーク**」が優位に働きやすい傾向が
> うかがえます。情報を整理し、衝動を抑え、長期的な目標に向かって
> 行動する傾向があるかもしれません。

- 「実行制御ネットワーク」は neuro_top3 の TOP1 を動的に表示。
- 「優位に働きやすい傾向」「〜かも」＝推定表現を厳守。

### C. タイプ名は既存のまま（改名しない）

- いきなり16個の改名は、既存のタイプ一覧・詳細・共有・前後ナビ等すべてに波及し、
  リスクが大きい。今回は「脳マップ」で"脳感"を出すことに集中する。
- タイプ名の「役割風」（思考のアーキテクト等）への改名は、別の機会に
  （効果を見てから）判断する。

## やらないこと

- タイプ名の一括改名（今回は見送り）
- 「医学的な脳タイプを診断」という誤認を招く表現（断定はしない）

## 承認プロセス

1. この仕様でOKか → OKなら実装
2. 実装 → ブラウザ確認 → スクリーンショットでレビュー
3. 気に入らなければ調整

※ この仕様は「提案の叩き台」です。タイプ名改名も含めて、範囲を狭めて
   確実に進めるための第一歩です。