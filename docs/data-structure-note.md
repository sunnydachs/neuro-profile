# データ構造の整理（命名刷新の前提確認）

> 命名刷新を安全に進めるため、現状の「神経システム」データがどこにどう存在し、
> 診断結果とタイプ詳細でどう使われるかを整理した。

## 現状の「神経システム」の2つの存在

### 1. 動的計算（診断結果用）— scoring.js
- `neuroScores()` が axis_meta.json の `neuro_systems[].weights` × 軸スコアで計算。
- 診断を実際に受けた時の「あなたの神経システム TOP3」はこれで決まる。
- つまり**毎回ユーザーの回答から計算される**。ユーザーごとに変わる。

### 2. 静的データ（タイプ詳細用）— types.json の neuro_top3
- 各タイプに「このタイプの代表的な神経システム」を3つハードコード。
- タイプ一覧・詳細ページ（type.html）で表示される。
- ユーザーの回答に関係なく、タイプごとに固定。

## 問題

この2つが「別物」であるため：
- 診断結果で「報酬系がトップ」と出ても、タイプ詳細に飛ぶと（静的）neuro_top3 と
  食い違う可能性がある。
- 命名刷新（分類を先に固めて割り当て直す）を行うと、この2つをどう整合させるかが
  論点になる。

## 方針案（推薦）

命名刷新は「3つの層」に分けて、科学的根拠を担保しつつ進める：

### 層A：神経システム本体（例：報酬・動機づけ系、実行制御ネットワーク…）
→ axis_meta.json の neuro_systems。これは**動的計算の根拠**だから変更しない
（weights も label も region も現状維持）。

### 層B：タイプの「分類グループ」（例：SALIENCE系 / EXECUTIVE系 / DEFAULT系 / SOCIAL系）
→ 新しい概念。あなたの提案の「系統」に相当。各タイプをこの4系統に振り分ける。
→ これは**新規に定義**する（既存の探索/警戒×直感/分析の4グループとは別物として）。

### 層C：各タイプの名前（NEURAL NAME / JAPANESE TYPE）
→ 層B の分類に合わせて命名。

## 重要な注意

「BRAIN SYSTEM / CORE REGION」として各タイプに何を表示するかは：
- 動的（診断結果）では → 実際の neuroScores 計算から出す
- 静的（タイプ詳細）では → types.json の neuro_top3 を使う

この2つを**一致させる**のが理想だが、現状は別設計。命名刷新と同時に「静的 neuro_top3 を
動的計算の代表値に揃える」作業が必要になりうる。

## 次の一手（提案）

1. まず「層B（分類グループ）を4系統で確定」する。
2. 各タイプを4系統に振り分ける（既存の軸構成から機械的に or 手作業で）。
3. その振り分けに合わせて NEURAL NAME / JAPANESE TYPE を命名。
4. 静的 neuro_top3 は、診断ロジックと矛盾しないよう「そのタイプの軸構成から
   導出される神経システム」で再構築する（軸構成→weights→neuro の関係を保つ）。

※ この整理を踏まえて、命名案を再度精査する。